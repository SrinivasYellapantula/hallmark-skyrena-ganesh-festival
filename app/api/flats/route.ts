import { getD1 } from "../../../db"; import { ensureDatabase } from "../../../db/initialize";
import { authorize, scopedBlock } from "../../lib/auth"; import { BLOCKS, EVENT_ID } from "../../lib/constants"; import { cleanText } from "../../lib/server";
export async function GET(request:Request){const auth=await authorize(request);if("response"in auth)return auth.response;await ensureDatabase();const url=new URL(request.url);const block=scopedBlock(auth.user,url.searchParams.get("block"));if(!BLOCKS.includes(block as never))return Response.json({flats:[],user:auth.user});const rows=await getD1().prepare(`SELECT id,block_no blockNo,flat_no flatNo,resident_name residentName,occupancy,visit_status visitStatus,visit_notes visitNotes,last_visited_at lastVisitedAt FROM flats WHERE event_id=? AND block_no=? AND occupied=1 AND visit_status IN ('pending','visit_again','visited','opted_out') ORDER BY flat_no`).bind(EVENT_ID,block).all();return Response.json({flats:rows.results,user:auth.user,block});}
export async function POST(request:Request){const auth=await authorize(request);if("response"in auth)return auth.response;const body=await request.json() as Record<string,unknown>;const block=scopedBlock(auth.user,body.blockNo);const flat=cleanText(body.flatNo,20).toUpperCase();const residentName=cleanText(body.residentName,100);const requestedOccupancy=cleanText(body.occupancy,10).toLowerCase();const occupancy=["owner","tenant"].includes(requestedOccupancy)?requestedOccupancy:"";if(!BLOCKS.includes(block as never)||!flat)return Response.json({error:"Block and flat are required."},{status:400});await ensureDatabase();const d1=getD1();await d1.prepare(`INSERT INTO flats(id,event_id,block_no,flat_no,resident_name,occupancy,occupied,visit_status,updated_by) VALUES(?,?,?,?,?,?,1,'pending',?) ON CONFLICT(event_id,block_no,flat_no) DO UPDATE SET occupied=1,resident_name=CASE WHEN excluded.resident_name<>'' THEN excluded.resident_name ELSE flats.resident_name END,occupancy=CASE WHEN excluded.occupancy<>'' THEN excluded.occupancy ELSE flats.occupancy END,updated_by=excluded.updated_by,updated_at=CURRENT_TIMESTAMP`).bind(crypto.randomUUID(),EVENT_ID,block,flat,residentName,occupancy,auth.user.username).run();await d1.prepare(`INSERT INTO audit_log(id,entity_type,entity_id,action,actor,details) VALUES(?,'occupied_flat',?,'added_or_updated',?,?)`).bind(crypto.randomUUID(),`${EVENT_ID}:${block}:${flat}`,auth.user.username,JSON.stringify({block,flat,residentName,occupancy})).run();return Response.json({ok:true,blockNo:block,flatNo:flat},{status:201});}
export async function PATCH(request:Request){const auth=await authorize(request);if("response"in auth)return auth.response;const body=await request.json() as Record<string,unknown>;const id=cleanText(body.id,80);const status=cleanText(body.visitStatus,20);const notes=cleanText(body.visitNotes,500);if(!id||!["pending","visited","visit_again","opted_out"].includes(status))return Response.json({error:"Invalid flat update."},{status:400});await ensureDatabase();const row=await getD1().prepare(`SELECT block_no blockNo FROM flats WHERE id=?`).bind(id).first<{blockNo:string}>();if(!row||(auth.user.role==="block"&&row.blockNo!==auth.user.blockNo))return Response.json({error:"Flat not found."},{status:404});await getD1().prepare(`UPDATE flats SET visit_status=?,visit_notes=?,last_visited_at=CURRENT_TIMESTAMP,updated_by=?,updated_at=CURRENT_TIMESTAMP WHERE id=?`).bind(status,notes,auth.user.username,id).run();return Response.json({ok:true});}
export async function DELETE(request:Request){const auth=await authorize(request);if("response"in auth)return auth.response;const body=await request.json() as Record<string,unknown>;const block=scopedBlock(auth.user,body.blockNo);const flat=cleanText(body.flatNo,20).toUpperCase();if(!BLOCKS.includes(block as never)||!flat)return Response.json({error:"Block and flat are required."},{status:400});await ensureDatabase();const d1=getD1();const result=await d1.prepare(`UPDATE flats SET occupied=0,updated_by=?,updated_at=CURRENT_TIMESTAMP WHERE event_id=? AND block_no=? AND flat_no=? AND occupied=1`).bind(auth.user.username,EVENT_ID,block,flat).run();if(!result.meta.changes)return Response.json({error:"Occupied flat not found."},{status:404});await d1.prepare(`INSERT INTO audit_log(id,entity_type,entity_id,action,actor,details) VALUES(?,'occupied_flat',?,'removed',?,?)`).bind(crypto.randomUUID(),`${EVENT_ID}:${block}:${flat}`,auth.user.username,JSON.stringify({block,flat})).run();return Response.json({ok:true,blockNo:block,flatNo:flat});}
